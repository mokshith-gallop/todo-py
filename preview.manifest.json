{
  "services": {
    "backend": {
      "cwd": ".",
      "cmd": "sh",
      "args": ["-c", "python3 -m venv .venv && .venv/bin/pip install -e . && .venv/bin/alembic upgrade head && .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8080"],
      "port": 8080,
      "healthCheck": {
        "path": "/api/health",
        "timeoutSeconds": 180
      }
    }
  },
  "routes": {
    "/api": { "service": "backend", "stripPrefix": false },
    "/": { "service": "backend", "stripPrefix": false }
  }
}
